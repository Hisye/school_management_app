import 'package:flutter/material.dart';

class AddStudentResult extends StatelessWidget {
  const AddStudentResult({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}